# Examples

Example assets for local testing and calibration.

- `calibration-sheets/` - calibration sheet photos used to validate image preprocessing and sheet detection behavior.

These files are not secrets and may be committed. Runtime uploads should stay under `data/feedback/feedback_data/session_uploads/`, which is ignored by git.
